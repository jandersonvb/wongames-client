import { Meta, StoryObj } from '@storybook/react';
import { Menu } from './Menu';

export default {
  title: 'Menu',
  component: Menu,
  parameters: {
    layout: 'fullscreen'
  }
} as Meta;


export const Default: StoryObj = {}